pre-commit:
  jobs:
    - run: pnpm exec dotenvx precommit
    - run: pnpm run fix --no-error-on-unmatched-pattern -- {staged_files}
      glob:
        - "{,**/}*.{js,jsx,ts,tsx,json,jsonc,css,md,mdx}"
      stage_fixed: true
